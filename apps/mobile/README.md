# @mycoupon/mobile

React Native (Expo) 앱 — 아직 초기화되지 않은 셸입니다.

## 타입 참조
- `@mycoupon/api` → `packages/api` → `server/routers` AppRouter 타입 터널
- `@shared/*`    → `shared/` 공유 유틸리티

## 초기화 방법 (향후 작업)
```bash
npx create-expo-app@latest . --template blank-typescript
```
