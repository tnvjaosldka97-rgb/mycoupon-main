# 공급망 공격 방어 (2026-05-15): Railway 빌드가 lockfile을 갱신하지 못하게 강제.
# 효과: 빌드 시 lock 그대로만 설치 → 갓 발행된 악성 버전 유입 차단 (구멍 #1 운영측).
# 조건: pnpm-lock.yaml ↔ package.json 정합 필수 (현재 121/121 정합 — raw 검증 완료).
# pnpm은 CI 환경에서 이미 기본 frozen — 본 설정은 명시적 영구 보장(심층 방어).
frozen-lockfile=true
